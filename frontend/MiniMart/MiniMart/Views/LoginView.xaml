<Window x:Class="MiniMart.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MiniMart.ViewModels"
        Title="Đăng Nhập - POS" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6"
        FontFamily="Segoe UI"
        MouseDown="Window_MouseDown">
    
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <!-- Custom Window Chrome -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" CornerRadius="0" GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Grid>
        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#4F46E5" Offset="0.0" />
                <GradientStop Color="#7C3AED" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Window Controls -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Panel.ZIndex="100">
            <Button Content="—" Width="35" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" FontWeight="Bold" Cursor="Hand" Click="MinimizeButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="✕" Width="35" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" FontWeight="Bold" Cursor="Hand" Click="CloseButton_Click" Margin="5,0,0,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EF4444"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Decorative Circles -->
        <Ellipse Width="400" Height="400" Fill="#20FFFFFF" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-100,-100,0,0" />
        <Ellipse Width="300" Height="300" Fill="#10FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-50,-50" />

        <Border Background="White" Width="500" VerticalAlignment="Center" HorizontalAlignment="Center"
                CornerRadius="16" Padding="40,50,40,40">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="20" ShadowDepth="10" />
            </Border.Effect>
            
            <Grid>
                <!-- Loading Overlay -->
                <Grid Panel.ZIndex="10" Background="#CCFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Đang đăng nhập..." FontSize="16" Foreground="#4F46E5" FontWeight="SemiBold" />
                    </StackPanel>
                </Grid>

                <StackPanel>
                    <!-- Header -->
                    <TextBlock Text="MiniMart POS" FontSize="32" FontWeight="Bold" Foreground="#111827" HorizontalAlignment="Center" Margin="0,0,0,8" />
                    <TextBlock Text="Đăng nhập tài khoản" FontSize="14" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,0,0,40" />

                    <!-- Error Message -->
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontSize="13" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Username -->
                    <TextBlock Text="Tên đăng nhập" FontSize="14" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8" />
                    <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Background="#F9FAFB" Padding="12,10" Margin="0,0,0,20">
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" FontSize="15" Foreground="#111827" />
                    </Border>

                    <!-- Password -->
                    <TextBlock Text="Mật khẩu" FontSize="14" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8" />
                    <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Background="#F9FAFB" Padding="12,10" Margin="0,0,0,30">
                        <PasswordBox x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" Background="Transparent" BorderThickness="0" FontSize="15" Foreground="#111827" />
                    </Border>

                    <!-- Login Button -->
                    <Button Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                            Background="#4F46E5" Foreground="White" FontSize="16" FontWeight="Bold" Padding="0,14" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        Đăng Nhập
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
